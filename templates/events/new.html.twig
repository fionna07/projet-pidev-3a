{% extends 'base.html.twig' %}

{% block title %}Créer un nouvel événement{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4">Créer un nouvel événement</h1>
    <div class="row">
        <div class="col-md-8 offset-md-2">
            {{ include('events/_form.html.twig', {'button_label': 'Créer'}) }}
        </div>
    </div>
    <div class="mt-3">
        <a href="{{ path('app_events_index') }}" class="btn btn-primary">Retour à la liste</a>
    </div>
</div>
{% endblock %}
