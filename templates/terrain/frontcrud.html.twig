{% extends 'base.html.twig' %}

{% block title %}Nos Terrains{% endblock %}

{% block body %}
<div class="container my-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-center">Nos Terrains</h1>
        <!-- Bouton pour ajouter un terrain -->
        <a href="{{ path('app_terrain_newfront') }}" class="btn btn-primary">
            <i class="fas fa-plus-circle mr-2"></i>Ajouter un terrain
        </a>
    </div>
    <div class="row">
        {% for terrain in terrains %}
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    {% if terrain.photos %}
                        <img src="{{ asset('uploads/terrains/' ~ terrain.photos) }}" 
                             class="card-img-top" 
                             alt="Photo du terrain" 
                             style="height: 200px; object-fit: cover;">
                    {% else %}
                        <img src="{{ asset('images/default-terrain.png') }}" 
                             class="card-img-top" 
                             alt="Aucune image" 
                             style="height: 200px; object-fit: cover;">
                    {% endif %}
                    <div class="card-body">
                        <h5 class="card-title">{{ terrain.adresse }}</h5>
                        <p class="card-text">
                            <strong>Surface :</strong> {{ terrain.surface|number_format(2, ',', ' ') }} m²<br>
                            <strong>Prix :</strong> {{ terrain.prix|number_format(2, ',', ' ') }} TND<br>
                            <strong>Type de sol :</strong>
                            <span class="badge 
                                {% if terrain.typeSol == 'argileux' %}badge-danger
                                {% elseif terrain.typeSol == 'sablonneux' %}badge-warning
                                {% elseif terrain.typeSol == 'limoneux' %}badge-success
                                {% else %}badge-secondary{% endif %}">
                                {{ terrain.typeSol }}
                            </span><br>
                            <strong>Statut :</strong>
                            <span class="badge badge-{{ terrain.status == 'disponible' ? 'success' : 'warning' }}">
                                {{ terrain.status }}
                            </span>
                        </p>
                    </div>
                    <div class="card-footer text-center">
                        <a href="{{ path('app_terrain_edit', {'id': terrain.id}) }}" class="btn btn-primary btn-sm">
                            Modifier
                        </a>
                        <form method="post" action="{{ path('app_terrain_delete', {'id': terrain.id}) }}" style="display:inline-block;" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce terrain ?');">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ terrain.id) }}">
                            <button class="btn btn-danger btn-sm" type="submit">Supprimer</button>
                        </form>
                        <!-- Bouton pour ouvrir le modal de création de transaction -->
                        <button type="button" class="btn btn-success btn-sm" data-toggle="modal" data-target="#transactionModal{{ terrain.id }}">
                            Faire Action
                        </button>
                    </div>
                </div>
            </div>

            {# Modal pour la transaction pour ce terrain #}
            <div class="modal fade" id="transactionModal{{ terrain.id }}" tabindex="-1" role="dialog" aria-labelledby="transactionModalLabel{{ terrain.id }}" aria-hidden="true">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="transactionModalLabel{{ terrain.id }}">
                                Créer une Transaction pour {{ terrain.adresse }}
                            </h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Fermer">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            {# Inclusion du formulaire de transaction en passant la variable terrain #}
                            {% include 'transaction/transac.html.twig' with { 'terrain': terrain } %}
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <p class="text-center text-muted">Aucun terrain trouvé</p>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
